<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- ComfyUI on LAN often uses HTTP; for public internet use HTTPS + a proxy -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
